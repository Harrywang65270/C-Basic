预处理命令不是c++本身的组成部分，更不是c++语句，
它是c++标准规定的可以出现在c++源程序文件中的命令。

宏定义
在c++源程序中允许用一个标识符来代表一个字符文本，称为宏，标识符为宏名。


不带字符的宏定义：
#define 宏名 字符文本
例子：

#define PI 3.141593
L =2*PI*r;

#define PI y*y+5*y
S = 3*M + 4*M + 5*M;

宏定义只是简单置换，不作语法检查，因此，宏串中的每个字符都是有效字符；

下面的例子是多余字符均导致编译出错：

#define PI "3.141592"

#define PI 3.141592;

